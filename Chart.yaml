apiVersion: v2
name: diemer-codes
description: diemer.codes — Astro personal site (phosphor edition), static build served by nginx
type: application
version: 2.1.0
# appVersion tracks the image tag in values.yaml (image.tag).
appVersion: "2.1.0"
maintainers:
  - name: zachd

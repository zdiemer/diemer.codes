apiVersion: v1
kind: Service
metadata:
  name: {{ include "diemer-codes.fullname" . }}
  labels:
    {{- include "diemer-codes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "diemer-codes.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
